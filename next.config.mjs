/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.ctfassets.net', 'platform-lookaside.fbsbx.com', "lh3.googleusercontent.com"]
      },
};

export default nextConfig;
